server:
  port: 8083

spring:
  application:
    name: sk2face-match-service

  datasource:
    url: jdbc:mysql://match-mysql:3306/sk2face_match
    username: sk2face
    password: sk2face123
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      connection-timeout: 30000
      initialization-fail-timeout: 0

  jpa:
    hibernate:
      ddl-auto: update

  flyway:
    enabled: true
    locations: classpath:db/migration

eureka:
  client:
    service-url:
      defaultZone: http://registry:8761/eureka/